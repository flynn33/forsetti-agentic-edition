# Known Limitations

- PowerShell validator execution could not be run because no `pwsh`, `powershell`, or `powershell.exe` host is available on PATH. The validator was updated and statically checked, and dependency-free local acceptance checks were run.
